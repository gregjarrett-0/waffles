import * as BrandExports from '@datacamp/waffles/brand';

import Layout from '../../components/content-layout';
import BestPractices from '../../components/best-practices';
import BrandGrid from '../../components/brand-grid';
import Example from '../../components/example';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import BasicUsage from '../../examples/brand/basic-usage';
import CustomSize from '../../examples/brand/custom-size';
import InvertedMonochrome from '../../examples/brand/inverted-monochrome';
import Inverted from '../../examples/brand/inverted';
import Monochrome from '../../examples/brand/monochrome';

<Layout title="Brand" description="A selection of logos and logomarks for the DataCamp Brand">

## Usage guidance

Use one of the available DataCamp logos or logomarks when you have a need to represent or show the brand, e.g. in the footer or header of a page. Visual usage of the DataCamp brand should not stray from those available below.

All `Brand` components are **responsive** by default and will fit to their parent container if no `width` or `height` is provided (whilst still adhering to their `viewBox` aspect ratio). Please note, both properties should not be provided together as this will break the responsive scaling behavior.

The original `SVG` files are available for download at the bottom of the [overview](#available-datacamp-logos).

## DataCamp Logos

<BrandGrid brandType="DataCamp" />

## Certification Logos

<BrandGrid brandType="Certification" />

## Jobs Logos

<BrandGrid brandType="Jobs" />

## Learn Logos

<BrandGrid brandType="Learn" />

## Workspace Logos

<BrandGrid brandType="Workspace" />

## Examples

<Example title="Basic usage" path="brand/basic-usage">
  <BasicUsage />
</Example>

<Example title="Custom size" path="brand/custom-size">
  <CustomSize />
</Example>

<Example title="Monochrome" path="brand/monochrome">
  <Monochrome />
</Example>

<Example title="Inverted" path="brand/inverted" darkPreview>
  <Inverted />
</Example>

<Example
  title="Inverted and monochrome"
  path="brand/inverted-monochrome"
  darkPreview
>
  <InvertedMonochrome />
</Example>

## Props

### Brand

In addition to `inverted` and `monochrome` listed below, Brand components accept all props a regular `SVGElement` would, such as `width`, `height`, `aria-` and `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/brand/datacamp/data-camp-logo')}
/>

## Imports

You can import the following components from this module:

<Imports module={BrandExports} path="@datacamp/waffles/brand" />

</Layout>
