import * as HelpersExports from '@datacamp/waffles/helpers';

import Layout from '../../components/content-layout';
import Example from '../../components/example';
import Imports from '../../components/imports';

import MediaQueryBasicUsage from '../../examples/helpers/media-query-basic-usage';
import HexToRgbaBasicUsage from '../../examples/helpers/hex-to-rgba-basic-usage';
import ReadableHexColorBasicUsage from '../../examples/helpers/readable-hex-color-basic-usage';
import ReadableHexColorCustomColors from '../../examples/helpers/readable-hex-color-custom-colors';
import HexColorShadeBasicUsage from '../../examples/helpers/hex-color-shade-basic-usage';
import HexColorLuminanceBasicUsage from '../../examples/helpers/hex-color-luminance-basic-usage';
import IsHexColorBasicUsage from '../../examples/helpers/is-hex-color-basic-usage';
import DeepChildrenMapBasicUsage from '../../examples/helpers/deep-children-map-basic-usage';
import SetTitleCaseBasicUsage from '../../examples/helpers/set-title-case-basic-usage';

<Layout title="Helpers" description="Collection of utility functions, which standardize some common patterns across DataCamp codebase.">

## mediaQuery

Based on the **breakpoints** defined in [design tokens](/foundation/design-tokens) return media queries for various device viewport sizes: `aboveSmall` (tablets), `aboveMedium` (small desktops), and `aboveLarge` (wide screen).

Following mobile-first approach, when using multiple media queries make sure to write them in correct order: put the _mobile_ styles first (these are the ones without any media query specified), `aboveSmall` before `aboveMedium`, and `aboveMedium` before `aboveLarge`.

<Example title="Basic usage" path="helpers/media-query-basic-usage">
  <MediaQueryBasicUsage />
</Example>

## hexToRgba

Converts **HEX** color to **RGBA** counterpart, with optional alpha setting. Mainly used to change opacity of the color.

<Example title="Basic usage" path="helpers/hex-to-rgba-basic-usage">
  <HexToRgbaBasicUsage />
</Example>

## readableHexColor

For the best contrast and readability, depending whether provided **HEX** color is dark or light return `tokens.colors.white` or `tokens.colors.navy` from [design tokens](/foundation/design-tokens). It is also possible to specify custom colors.

<Example title="Basic usage" path="helpers/readable-hex-color-basic-usage">
  <ReadableHexColorBasicUsage />
</Example>

<Example title="Custom colors" path="helpers/readable-hex-color-custom-colors">
  <ReadableHexColorCustomColors />
</Example>

## hexColorShade

Makes **HEX** color darker or lighter without changing its opacity.

<Example title="Basic usage" path="helpers/hex-color-shade-basic-usage">
  <HexColorShadeBasicUsage />
</Example>

## hexColorLuminance

Calculates **HEX** color luminance. Useful for various accessibility color checks.

<Example title="Basic usage" path="helpers/hex-color-luminance-basic-usage">
  <HexColorLuminanceBasicUsage />
</Example>

## isHexColor

Checks whether color is provided in **HEX** notation or not. Examples of **HEX** colors: `#05192D`, `#22E`. Examples of colors in _different_ notations: `rgb(245, 16, 0)`, `rgba(16, 8, 36, 0.4)`, `hsl(100 100% 50%)`.

<Example title="Basic usage" path="helpers/is-hex-color-basic-usage">
  <IsHexColorBasicUsage />
</Example>

## deepChildrenMap

In a similar way to [React.Children.map](https://reactjs.org/docs/react-api.html#reactchildren), creates a new children array, but calls a provided function recursively on every child (and its children).

<Example
  title="Basic usage"
  path="helpers/deep-children-map-basic-usage"
  darkPreview
>
  <DeepChildrenMapBasicUsage />
</Example>

## setTitleCase

Converts provided text to _Title Case_. Waffles [Button](/components/button) uses this for provided string content by default.

<Example title="Basic usage" path="helpers/set-title-case-basic-usage">
  <SetTitleCaseBasicUsage />
</Example>

## Imports

You can import following components or utilities from this module:

<Imports module={HelpersExports} path="@datacamp/waffles/helpers" />

</Layout>
