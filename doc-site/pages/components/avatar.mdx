import * as AvatarExports from '@datacamp/waffles/avatar';

import Layout from '../../components/content-layout';
import BestPractices from '../../components/best-practices';
import Example from '../../components/example';
import Playground from '../../components/playground';
import PropsTable from '../../components/props-table';
import Imports from '../../components/imports';

import playgroundConfig from '../../examples/avatar/playground-config';
import Variants from '../../examples/avatar/variants';
import DarkVariants from '../../examples/avatar/variants-dark';
import Sizes from '../../examples/avatar/sizes';
import BasicUsage from '../../examples/avatar/basic-usage';
import ContentFill from '../../examples/avatar/content-fill';

<Layout title="Avatar" description="Visually represents the identity of a user, group or entity.">

## Usage guidance

Use `Avatars` to help identify a user, entity or group within a page, alongside additional information about what the `Avatar` represents. Visual-based `content` should be prioritized over text.

If an `Avatar` is used with an image of a user or individual, it should fallback to a placeholder image (for example the `AvatarPlaceholderColored` [Asset](/components/asset/#other-assets)) or the initial of the name when this image is unavailable. For smaller `size` variants, text-based `content` should always be used where an image is harder to identify.

`Avatar` is available in a variety of colors, via the `variant` prop. The `white` and `greyLight` variants should only be used on dark backgrounds, such as `navy`. These colors of a `Avatar` should never be overwritten, as they are chosen for suitable accessibility and brand requirements.

Similarly, for smaller `Avatars` (below and including `small`) where an [Asset](/components/asset) is used for `content`, a relevant [Icon](/components/icon) should be used instead.

## Best practices

<BestPractices>
  <BestPractices.Recommended>

    - choose an appropriate content type for the specific Avatar size
    - prioritize visual-based `content`, where available
    - use `contentFill` when you have content that does not have a transparent background
    - use the correct `variant` for the background color

  </BestPractices.Recommended>
  <BestPractices.Discouraged>

    - use images or assets for very small Avatars&mdash;use a single character or an [Icon](/components/icon) instead
    - use text for `content`, where a visual element is available&mdash;except for smaller sizes
    - use `contentFill` when using an [Icon](/components/icon) as content
    - use `white` or `greyLight` variants on a light background

  </BestPractices.Discouraged>
</BestPractices>

## Examples

<Example title="Basic usage" path="avatar/basic-usage">
  <BasicUsage />
</Example>

<Example title="Variants" path="avatar/variants">
  <Variants />
</Example>

<Example
  title="Dark background-only variants"
  path="avatar/variants-dark"
  darkPreview
>
  <DarkVariants />
</Example>

<Example title="Sizes" path="avatar/sizes">
  <Sizes />
</Example>

<Example title="Fill whole avatar space" path="avatar/content-fill">
  <ContentFill />
</Example>

## Playground

<Playground {...playgroundConfig} />

## Props

### Avatar

In addition to all custom props listed below, `Avatar` accepts all props regular `HTMLDiv` would, such as `aria-labelledby`, `aria-describedby`, `data-` attributes.

<PropsTable
  metadata={require('!!extract-react-types-loader!../../../src/avatar/avatar')}
/>

## Imports

You can import following components or utilities from this module:

<Imports module={AvatarExports} path="@datacamp/waffles/avatar" />

</Layout>
